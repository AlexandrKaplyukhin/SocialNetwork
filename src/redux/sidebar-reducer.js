let initilaState = [{ id: 1, name: 'Alex' }, { id: 2, name: 'Eva' }, { id: 3, name: 'Kirill' }]

const sidebarReducer = (state = initilaState, action) => {

    return state;
}

export default sidebarReducer;